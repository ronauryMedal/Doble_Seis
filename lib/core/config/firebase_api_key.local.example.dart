/// Copia este archivo como `firebase_api_key.local.dart` y pega la API key
/// de `android/app/google-services.json` → `client[].api_key[].current_key`.
///
/// Ese `.local.dart` está en `.gitignore` y no se sube al repositorio.
const String firebaseAndroidApiKeyLocal = '';
