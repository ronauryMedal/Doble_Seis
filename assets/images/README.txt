Coloca aquí tu logo:

- `logo.png` — logo dentro de la app (Home, recomendado ~512×512, fondo transparente)
- `app_icon.png` — icono del launcher (cuadrado 1024×1024, sin bordes redondeados)

Luego ejecuta:

```powershell
flutter pub get
dart run flutter_launcher_icons
flutter run
```
